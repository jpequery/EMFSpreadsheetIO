package org.obeonetwork.spreadsheet.exporter;

public interface IRegisteredExcelExporter {
	/**
	 * return the name of the exporter, as seen in the export
	 * user interface
	 * @return
	 */
	String getName();
}
